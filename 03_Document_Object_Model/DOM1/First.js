/*let para = document.querySelector('p');

console.log(para);//It will print the first p tag it finds in the HTML file

para = document.querySelector('.error');
console.log(para);//It will print the first tag where this class is mentioned


//To select all elements with same occurrence in a HTML file
const paras = document.querySelectorAll('p');
console.log(paras)

//To get elements by their ID
const title = document.getElementById('page-title');
console.log(title);

//To get elements by their class names
const errors = document.getElementsByClassName('error');
console.log(errors);

//To get elements by their tag name
const paragraph = document.getElementsByTagName('p');
console.log(paragraph);
console.log(paragraph[2]); //Some of array methods are applicable to HTML collection also
//NOTE: Not all the array methods are applicable to the HTML collections and Nodelist;
*/